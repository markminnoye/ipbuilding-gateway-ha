{
  "config": {
    "step": {
      "user": {
        "title": "IPBuilding Gateway",
        "description": "Enter the IP address and port of the IPBuilding Gateway.",
        "data_schema": {
          "host": "Host",
          "port": "Port"
        },
        "errors": {
          "gateway_unreachable": "Could not reach the gateway at {host}:{port}. Is it running?",
          "connection_refused": "Connection refused — is the gateway running?"
        }
      }
    },
    "abort": {
      "already_configured": "This gateway is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "IPBuilding Gateway Options"
      }
    }
  },
  "button": {
    "pressed": {
      "message": "IPBuilding button {hardware_id} was {action}",
      "fields": {
        "hardware_id": "Hardware ID",
        "action": "Action"
      }
    }
  }
}